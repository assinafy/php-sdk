<?xml version="1.0" encoding="UTF-8"?>
<ruleset name="Assinafy PHP SDK"
         xmlns="http://pmd.sf.net/ruleset/1.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://pmd.sf.net/ruleset/1.0.0 http://pmd.sf.net/ruleset_xml_schema.xsd">
    <description>Static design and maintainability rules for the SDK.</description>

    <rule ref="rulesets/cleancode.xml">
        <exclude name="BooleanArgumentFlag"/>
        <exclude name="ElseExpression"/>
        <exclude name="ErrorControlOperator"/>
        <exclude name="MissingImport"/>
        <exclude name="StaticAccess"/>
    </rule>

    <rule ref="rulesets/codesize.xml">
        <exclude name="ExcessiveClassLength"/>
        <exclude name="ExcessiveMethodLength"/>
    </rule>

    <!--
        Length is measured in executable lines, not raw lines. Both rules default to PDepend's
        `loc`, which counts docblocks, so a thoroughly documented class trips them without
        gaining a single statement. `ignore-whitespace` switches the metric to `eloc`, which is
        what "this is trying to do too much" actually means. Thresholds are otherwise the
        upstream defaults.
    -->
    <rule ref="rulesets/codesize.xml/ExcessiveClassLength">
        <properties>
            <property name="ignore-whitespace" value="true"/>
        </properties>
    </rule>
    <rule ref="rulesets/codesize.xml/ExcessiveMethodLength">
        <properties>
            <property name="ignore-whitespace" value="true"/>
        </properties>
    </rule>

    <rule ref="rulesets/controversial.xml"/>
    <rule ref="rulesets/design.xml"/>
    <rule ref="rulesets/naming.xml">
        <exclude name="LongVariable"/>
        <exclude name="ShortVariable"/>
    </rule>
    <rule ref="rulesets/unusedcode.xml"/>
</ruleset>
